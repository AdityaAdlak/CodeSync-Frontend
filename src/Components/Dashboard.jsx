import DashMode from "./Mode";
import CodeTypingAnimation from "./codeRunner";

export default function Dashboard({toggleDarkMode,darkMode})
{
    return (
        <div>
            <CodeTypingAnimation/>
        </div>
    )
}